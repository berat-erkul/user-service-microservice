spring:
  profiles:
    active: default     #it adds -dev so -->  user-service-dev.yml
  cloud:
    config:
      name: user-service    # (-dev)
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 10
        max-interval: 2000
